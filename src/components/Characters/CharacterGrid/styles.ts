import { SxProps } from '@mui/material';

const CharactersGrid: Record<string, SxProps> = {
  container: {
    display: 'flex',
    flexDirection: 'column',
    flex: 1,
    flexGrow: 1,
    p: 2,
  },
};

export default CharactersGrid;
